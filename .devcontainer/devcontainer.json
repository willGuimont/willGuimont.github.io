{
    "name": "willGuimont website",
    "dockerComposeFile": [
        "../docker-compose.yaml"
    ],
    "workspaceFolder": "/root/",
	// "workspaceMount": "source=${localWorkspaceFolder},target=/workspaces/project,type=bind",
    "service": "dev-linux", // The service in docker-compose.yml that we want vs code to use as a dev containers 
    "shutdownAction": "stopCompose",
    "customizations": {
        "vscode": {
            "extensions": []
        }
    }
}